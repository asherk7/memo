"""Evaluation harness (Phase 12, trimmed): metrics, robustness sweep, benchmark.

A measurement layer that *substantiates* pillar A (gated fusion + calibration)
and feeds pillar C (CPU inference). Not a pillar of its own — fairness audit,
cross-dataset transfer, bootstrap CIs, and reliability-diagram PNGs were cut.
"""
